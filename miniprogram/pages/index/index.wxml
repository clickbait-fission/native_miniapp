<template name="home">
    <tab-home hasTop="{{hasTop}}" top="{{top}}"/>
</template>

<template name="article">
    <tab-article/>
</template>

<template name="settings">
    <tab-settings/>
</template>

<view class="container" style="{{vars}}">
    <view wx:if="{{ready}}" class="content-container">
        <view wx:for="{{tabs}}" wx:key="tab" wx:if="{{item.create}}" class="{{item.className}}">
            <template is="{{item.tab}}" data="{{ hasTop: hasTop, top: top }}" />
        </view>
    </view>
    <view wx:if="{{!ready}}" class="loading-hint">
        <spin-anim />
    </view>
    <view class="tab">
        <view wx:for="{{tabs}}" wx:key="tab" class="item" bind:tap="onTapTab" data-tab="{{item.tab}}">
            <image class="icon" mode="aspectFit"
                   src="{{item.tab === currentTab ? item.activeIcon : item.inactiveIcon}}"/>
            <view class="name">{{item.name}}</view>
        </view>
    </view>
</view>
